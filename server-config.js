const config = {
    database: {
        host:      'localhost',
        user:       'root',
        password: 'root',
        port:       3306,
        db:       'deepspace9'
    },
    server: {
        host: '127.0.0.1',
        port: '8000'
    }
}

// to retrieve in require call
module.exports = config